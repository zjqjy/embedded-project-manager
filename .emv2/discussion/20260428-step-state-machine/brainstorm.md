# 头脑风暴 - 步骤状态自动推进

## 确认方案

### Q1: 推进层级
- **子步骤间自动推进**: S7-A(完成) → Meta 区自动推进到 S7-B(开发中)
- 子步骤内部状态表和 Meta 区同步维护

### Q2: verify 触发状态变更
- 用户执行 `/em verify s7-a` → 子步骤状态 `🚧 开发中` → `🔄 验证中`
- Meta 区同步更新

### Q3: 返工流程
- 失败 → `🔄 验证中` → `🔁 返工中`
- 修复后再验证 → `🔁 返工中` → `🔄 验证中`
- 通过 → `🔄 验证中` → `✅ 完成`，同时推进到下一子步骤
